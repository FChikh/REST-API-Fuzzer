#%RAML 1.0 Library

uses:
  error: !include ../schemas/error.raml

types:
  ReasonUsageItemQuotaExceeded:
    description: 'Usage item quota for {{usage_item_id}} is exceeded'
    type: error.error
    properties:
      domain:
        type: string
        enum:
          - Licensing
        required: true
      reason:
        type: string
        enum:
          - ReasonUsageItemQuotaExceeded
        required: false
      code:
        type: string
        enum:
          - ReasonUsageItemQuotaExceeded
        required: true
      context:
        type: object
        properties:
          usage_item_id:
            type: string
            required: true
        required: true
